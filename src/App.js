import React, { Component } from "react";
import Score from "./components/ScoreBar";
import Jumbotron from "./components/PicWindow";
import Pictures from "";


class App extends Component {

  state = 
  {
    pictures,
    score: 0,
    highScore: 0,
    picturesClicked: [],
    text: "Hey There! Try to pick all of the actors head shots without picking the same one twice. Pick a Pic to get going."
  };

  
}